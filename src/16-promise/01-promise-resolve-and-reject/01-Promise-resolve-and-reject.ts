// ✅ Creates a promise that resolves with a given value
const ready = Promise.resolve("I'm done!");
// ✅ Creates a promise that rejects with a given reason
const error = Promise.reject("Something went wrong");
